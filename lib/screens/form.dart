import 'package:flutter/material.dart';

class DataForm extends StatelessWidget {
  String? _name, _adrees, _contact;
  final String? money, count, qua;
  DataForm({this.money, this.count, this.qua});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
